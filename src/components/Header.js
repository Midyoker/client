import React from 'react';
import CloudAnimation from './CloudAnimation/CloudAnimation'; // Adjust the path as per your project structure

function Header() {
  return (
    <header>
      <CloudAnimation />
    </header>
  );
}

export default Header;